package com.deepwork.focustimer.ui.theme

import androidx.compose.ui.graphics.Color

/** The only two ideas in the palette: pitch black and sepia. */
val PitchBlack = Color(0xFF000000)
val Sepia = Color(0xFFE6C280)
val SepiaDim = Color(0xFF8C7A50)   // muted sepia for secondary text / borders
val SepiaFaint = Color(0xFF3A3320) // very low-emphasis surfaces/dividers
